// 11yang.cpp : 定义控制台应用程序的入口点。
//

#include "stdafx.h"

#include <iostream> 
#include<math.h>
using namespace std;

void main()
{  
	cin>>n;
	if(n<2)
	{
		date error;
		break;
	}

	else
	{
		int i=0;
		for(int i=2;i<=sqat();i++;)
	{
		if(n%i==0);
		{
			break; 
		}
	}

	if(i==1)
	{
		cout<<"is prime.";

	}
	else if(i<n)
	{
		cout<<"isn't a prime.";
	}
		getchar();
		getchar();
}
